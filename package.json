{
  "name": "tw-minesweeper-client",
  "source": "src/index.html",
  "scripts": {
    "serve": "parcel",
    "build": "parcel build",
    "test": "jest",
    "lint:fix": "npm run lint:prettier && npm run lint:stylelint && npm run lint:eslint",
    "lint:prettier": "prettier --write src",
    "lint:stylelint": "stylelint 'src/**/*.css'",
    "lint:eslint": "eslint src"
  },
  "staticFiles": {
    "staticPath": "src/static",
    "staticOutPath": "static"
  },
  "devDependencies": {
    "eslint": "^9.24.0",
    "jest": "^29.7.0",
    "parcel": "^2.14.4",
    "parcel-reporter-static-files-copy": "^1.5.3",
    "prettier": "^3.5.3",
    "stylelint": "^16.18.0",
    "stylelint-config-standard": "^38.0.0"
  }
}
